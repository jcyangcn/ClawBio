name: clawbio-locuscompare
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.10
  - numpy>=1.24
  - scipy>=1.10
  - pandas>=1.5
  - matplotlib>=3.7
  - pysam>=0.22
  - pyyaml>=6.0
  - pydantic>=2.0
  - requests>=2.28
  # plink2 is optional (LD coloring); if installed, locuscompare uses it via
  # subprocess. plink2 is GPL-3 (subprocess only; no GPL contamination of the
  # MIT-licensed skill code).
  - plink2>=2.0
